{
  "name": "@wdip/extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node scripts/gen-icons.mjs && vite build",
    "dev": "node scripts/gen-icons.mjs && vite build --watch",
    "zip": "cd dist && powershell Compress-Archive -Path * -DestinationPath ../riftbound-recorder.zip -Force"
  },
  "dependencies": {
    "@wdip/shared": "workspace:*"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vite-plugin-web-extension": "^4.1.1",
    "@types/chrome": "^0.0.268",
    "typescript": "^5.4.5"
  }
}
